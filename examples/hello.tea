uint16_t main() // take no arguments, return 16 bit unsigned integer value
    const char8_t[? /* decide at compile time */] hello = "Hello Tea!"; 
    puts(hello,newline=true); // use standard puts function which takes a character array and newline is defaulted to false
    return 0;
end